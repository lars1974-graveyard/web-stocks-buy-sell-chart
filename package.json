{
  "scripts": {
    "install": "snowpack install",
    "start": "snowpack dev",
    "build": "snowpack build",
    "test": "snowpack dev --config snowpack.test-config.json"
  },
  "webDependencies": {
    "lit-element": "^2.3.1",
    "redux": "^4.0.5"
  },
  "devDependencies": {
    "snowpack": "^2.0.0-beta.14"
  }
}
