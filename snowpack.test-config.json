{
  "scripts": {
    "mount:test": "mount test --to /",
    "mount:src": "mount src --to /",
    "mount:web_modules": "mount web_modules",
    "build:js": "cat"
  },
  "installOptions": {},
  "devOptions": {}
}
